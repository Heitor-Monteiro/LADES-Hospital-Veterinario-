-- MySQL Script generated by MySQL Workbench
-- Sex 13 Mai 2016 00:30:20 BRT
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;

-- -----------------------------------------------------
-- Schema mydb
-- -----------------------------------------------------
-- -----------------------------------------------------
-- Schema BD_SIHV
-- -----------------------------------------------------

-- -----------------------------------------------------
-- Schema BD_SIHV
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `BD_SIHV` ;
USE `BD_SIHV` ;

-- -----------------------------------------------------
-- Table `BD_SIHV`.`pessoa`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `BD_SIHV`.`pessoa` (
  `CPF` BIGINT(11) NOT NULL,
  `RG` INT(7) NOT NULL,
  `NOME` VARCHAR(100) NOT NULL,
  `SEXO` SET('M', 'F') NULL,
  `LOGRA` VARCHAR(100) NOT NULL,
  `BAIRRO` VARCHAR(100) NOT NULL,
  `CIDADE` VARCHAR(100) NOT NULL,
  `CEP` INT(8) NOT NULL,
  `UF` SET('AC', 'AL', 'AP', 'AM', 'BA', 'CE', 'DF', 'ES', 'GO', 'MA', 'MT', 'MS', 'MG', 'PA', 'PB', 'PR', 'PE', 'PI', 'RN', 'RS', 'RJ', 'RO', 'RR', 'SC', 'SP', 'SE', 'TO') NOT NULL,
  `exclusaoLogica` TINYINT(1) NULL,
  PRIMARY KEY (`CPF`))
ENGINE = InnoDB
DEFAULT CHARACTER SET = latin1;


-- -----------------------------------------------------
-- Table `BD_SIHV`.`cliente`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `BD_SIHV`.`cliente` (
  `PK_cliente` INT NOT NULL AUTO_INCREMENT,
  `pessoa_CPF` BIGINT(11) NOT NULL,
  PRIMARY KEY (`PK_cliente`),
  INDEX `fk_cliente_pessoa1_idx` (`pessoa_CPF` ASC),
  CONSTRAINT `fk_cliente_pessoa1`
    FOREIGN KEY (`pessoa_CPF`)
    REFERENCES `BD_SIHV`.`pessoa` (`CPF`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `BD_SIHV`.`animais`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `BD_SIHV`.`animais` (
  `PK_animal` INT NOT NULL AUTO_INCREMENT,
  `cliente_pessoa_CPF` BIGINT(11) NOT NULL,
  `DIA` INT(2) NOT NULL,
  `MES` INT(2) NOT NULL,
  `ANO` INT(4) NOT NULL,
  `HORA` TIME(2) NOT NULL,
  `ESPECIE` VARCHAR(500) NOT NULL,
  `NOME` VARCHAR(50) NOT NULL,
  `RAÇA` VARCHAR(200) NOT NULL,
  `PELAGEM` VARCHAR(100) NOT NULL,
  `SEXO` SET('M', 'F') NOT NULL,
  `IDADE` INT(3) NOT NULL,
  `PESO` DOUBLE NOT NULL,
  INDEX `fk_animais_cliente1_idx` (`cliente_pessoa_CPF` ASC),
  PRIMARY KEY (`PK_animal`),
  CONSTRAINT `fk_animais_cliente1`
    FOREIGN KEY (`cliente_pessoa_CPF`)
    REFERENCES `BD_SIHV`.`cliente` (`pessoa_CPF`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE)
ENGINE = InnoDB
DEFAULT CHARACTER SET = latin1;


-- -----------------------------------------------------
-- Table `BD_SIHV`.`telefone`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `BD_SIHV`.`telefone` (
  `PK_telefone` INT NOT NULL AUTO_INCREMENT,
  `pessoa_CPF` BIGINT(11) NOT NULL,
  `numero` BIGINT(11) NOT NULL,
  PRIMARY KEY (`PK_telefone`),
  INDEX `fk_telefone_pessoa_idx` (`pessoa_CPF` ASC),
  CONSTRAINT `fk_telefone_pessoa`
    FOREIGN KEY (`pessoa_CPF`)
    REFERENCES `BD_SIHV`.`pessoa` (`CPF`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `BD_SIHV`.`usuario`
-- -----------------------------------------------------
CREATE TABLE IF NOT EXISTS `BD_SIHV`.`usuario` (
  `PK_usuario` INT NOT NULL AUTO_INCREMENT,
  `pessoa_CPF` BIGINT(11) NOT NULL,
  `adm_senha` VARCHAR(100) NOT NULL,
  `adm_login` VARCHAR(100) NOT NULL,
  PRIMARY KEY (`PK_usuario`),
  INDEX `fk_usuario_pessoa1_idx` (`pessoa_CPF` ASC),
  CONSTRAINT `fk_usuario_pessoa1`
    FOREIGN KEY (`pessoa_CPF`)
    REFERENCES `BD_SIHV`.`pessoa` (`CPF`)
    ON DELETE NO ACTION
    ON UPDATE CASCADE)
ENGINE = InnoDB;


SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
